{
  "description": "Shows all packages that have update candidates according to the current baseline",
  "input_method": "environment",
  "implementations": [
    {"name": "check_update.sh", "requirements": ["perl"]}
  ]
}
