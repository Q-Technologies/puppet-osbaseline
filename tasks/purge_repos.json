{
  "description": "Purges all repositories on a node",
  "input_method": "environment",
  "implementations": [
    {"name": "purge_repos.sh", "requirements": ["shell"]}
  ],
  "parameters": {
    "repo_dir": {
      "description": "Override the default OS repository directory",
      "type": "Optional[String[1]]"
    }
  }
}
